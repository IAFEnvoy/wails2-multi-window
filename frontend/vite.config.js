export default {
    // publicDir: './'
    build: {
        rollupOptions: {
            input: [
                'index.html', //TODO: Add extra pages here to let Vite pack
            ]
        }
    }
}